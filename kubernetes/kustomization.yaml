# ── LEGACY / DO NOT USE FOR NEW DEPLOYMENTS ─────────────────────────────────
# This kustomize tree references fictional microservice images
# (healthpoint/api-gateway:latest, healthpoint/nsa-idr-service:latest, ...)
# that do not exist as buildable artifacts in this repository. The supported
# Kubernetes path is the Helm chart at deploy/helm/healthpoint/ (pinned
# images, secrets by name only). This tree is retained for reference pending
# owner review.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: healthpoint

resources:
  - namespace.yaml
  - secrets/external-secrets.yaml
  - databases/postgresql.yaml
  - databases/redis.yaml
  - databases/kafka.yaml
  - databases/opensearch.yaml
  - databases/minio.yaml
  - auth/keycloak.yaml
  - auth/permify.yaml
  - workflows/temporal.yaml
  - ledger/tigerbeetle.yaml
  - gateway/apisix.yaml
  - microservices/api-gateway.yaml
  - microservices/nsa-idr-service.yaml
  - microservices/ai-services.yaml
  - frontend/web-app.yaml
  - ingress/ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: healthpoint
  app.kubernetes.io/managed-by: kustomize
