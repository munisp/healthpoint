# ARCHIVED — `idr-workflow-demo/` (stale full-app snapshot)

This directory is a **stale full-application snapshot**. It is **not built,
not deployed, and not maintained**. The canonical surfaces are:

- `client/` — the production React web application
- `server/` — the tRPC API server
- `mobile/` — the mobile application

Contents here are retained temporarily for reference only and are **pending
deletion**. Do not add new code, dependencies, or documentation here.
