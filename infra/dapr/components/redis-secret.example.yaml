# EXAMPLE ONLY — do not commit real credentials.
# Audit P1-13: statestore-redis.yaml references this secret. Copy to
# redis-secret.yaml (gitignored) or create the equivalent Kubernetes Secret
# via your secret manager:
#   kubectl create secret generic redis-secret \
#     --from-literal=password="$REDIS_PASSWORD"
apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: default
type: Opaque
stringData:
  password: "CHANGE_ME_redis_password"
