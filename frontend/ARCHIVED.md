# ARCHIVED — `frontend/` (stale Manus scaffold)

This directory is a **stale Manus-generated scaffold**. It is **not built, not
deployed, and not maintained**. The canonical frontend surfaces are:

- `client/` — the production React web application (routes in `client/src/App.tsx`)
- `mobile/` — the mobile application

Contents here are retained temporarily for reference only and are **pending
deletion**. Do not add new code, dependencies, or documentation here, and do
not wire any build/deploy tooling to this directory.
